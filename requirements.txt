# CPU-only torch wheels — keeps the deploy small enough for free hosting.
# (For local GPU training, install the normal CUDA build of torch instead.)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.2
torchvision==0.17.2

timm>=0.9.12
albumentations>=1.4.0
opencv-python-headless>=4.9.0
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
Pillow>=10.0.0
PyYAML>=6.0
grad-cam>=1.5.0
streamlit>=1.32.0
